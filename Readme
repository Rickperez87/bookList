Creating a book list that using Vanilla Javascript in an OOP way you can submit a form with book attributes that 
will create a table of books using static methods ect. Also uses local storage to store books. 